<Window x:Class="ClinicApp.Views.PatientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upravljanje Pacijentima" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- DataGrid za pacijente -->
        <DataGrid Grid.Row="0" 
                  Margin="0,0,10,10"
                  ItemsSource="{Binding Patients}"
                  SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Ime" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Prezime" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Datum Rođenja" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Broj Kartona" Binding="{Binding RecordNumber}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Forma za dodavanje -->
        <GroupBox Grid.Row="1" Header="Dodaj Pacijenta" Margin="10" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Ime:" Width="100"/>
                    <TextBox Width="150" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="Prezime:" Width="100"/>
                    <TextBox Width="150" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="Datum:" Width="80"/>
                    <DatePicker Width="120" SelectedDate="{Binding BirthDate, Mode=TwoWay}"/>
                    <Label Content="Broj Kartona:" Width="100"/>
                    <TextBox Width="100" Text="{Binding RecordNumber, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Dodaj" Command="{Binding AddPatientCommand}" Width="149"/>
                    <Button Content="Obriši Odabranog" Command="{Binding DeletePatientCommand}"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>